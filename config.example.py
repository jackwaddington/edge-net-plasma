# Copy to config.py on the device and fill in. Real secrets live in
# edge-net-secrets, never in git. config.py is gitignored.
WIFI_SSID     = "Pirie"
WIFI_PASSWORD = "CHANGE_ME"        # edge-net AP passphrase (see edge-net-secrets)
MQTT_BROKER   = "10.1.1.1"
MQTT_PORT     = 1883
LED_PIN       = "GP15"
NUM_LEDS      = 50
